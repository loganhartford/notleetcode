#include <vector>
#include <queue>

std::vector<int> buildOrder(int n, std::vector<std::vector<int>> deps) {
    // TODO: implement Kahn's topological sort
    // deps[i] = {a, b} means b must come before a
    // Return build order, or empty vector if cycle detected
    return {};
}
