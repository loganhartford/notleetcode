#include <stdlib.h>

/* Return a malloc'd array of length numsSize, and set *returnSize = numsSize.
 * The caller frees the returned array. No division allowed. */
int* productExceptSelf(int* nums, int numsSize, int* returnSize) {
    return NULL;
}
