#include <vector>
#include <string>
#include <algorithm>

std::vector<std::vector<std::string>> suggestedProducts(
    std::vector<std::string> products, std::string searchWord) {
    // TODO: sort products; for each prefix of searchWord, find up to 3 matches
    return {};
}
