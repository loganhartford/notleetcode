#include <vector>
#include <queue>

int shortestPath(std::vector<std::vector<int>>& grid) {
    // TODO: BFS from (0,0) to (n-1,m-1) using 8-directional movement
    // Return path length (counting endpoints), or -1 if no path
    return -1;
}
