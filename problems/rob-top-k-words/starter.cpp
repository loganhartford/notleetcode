#include <vector>
#include <string>
#include <unordered_map>
#include <algorithm>

std::vector<std::string> topKFrequent(std::vector<std::string> words, int k) {
    // TODO: count frequencies; sort by (freq desc, lex asc); return top k
    return {};
}
