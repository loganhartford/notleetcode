#include <stdint.h>

/* Implement these four macros. The stubs below compile but are wrong. */
#define SET_BIT(reg, n)     ((void)0)
#define CLEAR_BIT(reg, n)   ((void)0)
#define TOGGLE_BIT(reg, n)  ((void)0)
#define TEST_BIT(reg, n)    (0)
