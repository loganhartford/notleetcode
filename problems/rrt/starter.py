import math
import random


def rrt(grid, start, goal, max_iters=4000, step=1.0, goal_tol=1.5, seed=0):
    # Return a path [(x, y), ...] from start to within goal_tol of goal,
    # or None if no path is found within max_iters.
    pass
