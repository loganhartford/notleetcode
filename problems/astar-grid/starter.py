from typing import List


def astar(grid: List[List[int]], start: List[int], goal: List[int]) -> int:
    # grid: 0 = free, 1 = obstacle. 4-connectivity, unit step cost.
    # Use A* with the Manhattan-distance heuristic.
    # Return the shortest path length (steps), or -1 if unreachable.
    pass
