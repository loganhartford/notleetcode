#include <stdlib.h>

/* Return a malloc'd array holding the two indices, and set *returnSize = 2.
 * The caller frees the returned array. */
int* twoSum(int* nums, int numsSize, int target, int* returnSize) {
    return NULL;
}
