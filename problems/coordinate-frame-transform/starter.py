import math


def transform_point(pose, point):
    # pose = (x, y, theta); point = (px, py) in the frame. Return (x, y) in world.
    pass


def compose(pose_a, pose_b):
    # Return the composed pose (x, y, theta).
    pass


def invert(pose):
    # Return the inverse pose (x, y, theta).
    pass
