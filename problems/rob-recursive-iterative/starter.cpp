#include <vector>

struct ListNode {
    int val;
    ListNode* next;
    ListNode(int v, ListNode* n = nullptr) : val(v), next(n) {}
};

int listLength(ListNode* head) {
    // TODO: count nodes iteratively
    return 0;
}

std::vector<int> reverseValues(ListNode* head) {
    // TODO: collect values and return them in reverse order
    return {};
}
