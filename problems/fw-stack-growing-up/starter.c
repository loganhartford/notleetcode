#include <stdio.h>
#include <stdbool.h>

bool is_stack_growing_up(void) {
    /* hint: take a local's address here, call a noinline helper that takes
       its own local's address, and compare the two. */
    return false;
}
