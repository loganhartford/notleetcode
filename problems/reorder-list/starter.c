/* Definition for singly-linked list (provided by the test harness):
 *   struct ListNode { int val; struct ListNode* next; };
 *
 * Reorder the list in place. Return nothing.
 */

void reorderList(struct ListNode* head) {
}
