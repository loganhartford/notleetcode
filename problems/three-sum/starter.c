#include <stdlib.h>

/* Return a malloc'd array of triplets (each a malloc'd int[3]).
 * Set *returnSize to the number of triplets and *returnColumnSizes to a
 * malloc'd array where each entry is 3. The caller frees everything. */
int** threeSum(int* nums, int numsSize, int* returnSize, int** returnColumnSizes) {
    *returnSize = 0;
    return NULL;
}
