#include <string>
#include <stack>

int calculate(std::string s) {
    // TODO: evaluate +, -, *, / with correct precedence
    // Hint: use a stack; push values for +/-; apply */÷ immediately to stack top
    return 0;
}
