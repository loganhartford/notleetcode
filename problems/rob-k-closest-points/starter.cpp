#include <vector>
#include <algorithm>

std::vector<std::vector<int>> kClosest(std::vector<std::vector<int>> points, int k) {
    // TODO: return the k points closest to origin (0,0) by squared Euclidean distance
    return {};
}
