#include <vector>
#include <cmath>
#include <utility>

double polygonArea(std::vector<std::pair<double,double>> vertices) {
    // TODO: apply the Shoelace formula
    // sum = Σ (x_i * y_{i+1} - x_{i+1} * y_i), wrap around
    // area = |sum| / 2
    return 0.0;
}
