def bresenham(x0, y0, x1, y1):
    # Return the list of (x, y) cells from start to end, inclusive.
    pass


def line_of_sight(grid, start, end):
    # grid[y][x]: 1 = obstacle, 0 = free. start/end are (x, y).
    # Return True if no obstacle lies on the Bresenham line between them.
    pass
