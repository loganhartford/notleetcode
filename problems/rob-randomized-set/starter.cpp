#include <vector>
#include <unordered_map>
#include <cstdlib>

class RandomizedSet {
public:
    bool insert(int val) {
        // TODO: insert val if not present, return true; false if duplicate
        return false;
    }

    bool remove(int val) {
        // TODO: remove val if present, return true; false if absent
        // Hint: swap with last element for O(1) removal
        return false;
    }

    int getRandom() {
        // TODO: return uniformly random element from the set
        return 0;
    }
};
