#include <unordered_map>
#include <list>

class LRUCache {
public:
    LRUCache(int capacity) {
        // TODO: initialize with capacity
    }

    int get(int key) {
        // TODO: return value or -1; mark as recently used
        return -1;
    }

    void put(int key, int value) {
        // TODO: insert/update key; evict LRU if over capacity
    }
};
